import { defineConfig } from 'astro/config';

export default defineConfig({
  // Change this to your real domain once you're ready to go live,
  // e.g. site: 'https://darrenharrisgb.com'
  site: 'https://example.netlify.app',
});
